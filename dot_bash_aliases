#
# .bash_aliases
#

alias ls='ls --color=auto'
alias showcrt='openssl x509 -noout -text -in '
alias showcsr='openssl req -noout -text -in '
alias squidtime='/usr/bin/perl -p -e '\''s/^\d+\.\d+/localtime $&/e;'\'''

# end of file
